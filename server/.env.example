DATABASE_URL="postgresql://invoice:invoice@localhost:5433/invoice_dashboard"
DATABASE_SSL=false
# Set to false only if your Postgres host uses a self-signed cert (e.g. some managed providers)
# DATABASE_SSL_REJECT_UNAUTHORIZED=false
CORS_ORIGIN=http://localhost:5173
# USE_MOCK_DATA=true   # serve in-memory demo data; DATABASE_URL not required
# Optional in local dev; required in production. Generate with: openssl rand -hex 32
# API_KEY=
